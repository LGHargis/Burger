<h1>Burger Menu!</h1>

<h2>Menu:</h2>

<ul>
    {{#each burgers}} {{#unless devoured}} {{> burgers/burgers-block devoured=true}} {{/unless}} {{/each}}
</ul>

<h2>Burgers You Have Devoured!</h2>

<ul>
    {{#each burgers}} {{#if devoured}} {{> burgers/burgers-block devoured=false}} {{/if}} {{/each}}
</ul>

<h2>Add a burger to our menu!</h2>
<form class="create-form">
    <div class="form-group">
        <label for="ca">Burger Name:</label>
        <input type="text" id="ca" name="burger_name">
    </div>

    <div class="form-group">
        <label>New Burger Idea?</label><br>
        <input type="radio" name="devoured" value="0"> Put on Menu
    </div>

    <button type="submit">Add Burger</button>
</form>